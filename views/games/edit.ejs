<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Game Page</title>
</head>
<body>
    <h1>Edit <%= game.name %></h1>
    <form action="/games/<%= game.id%>?_method=PUT" method="POST">
        <label for="name">Name:</label>
        <input type="text" value="<%=game.name%>" name ="name"/>

        <label for="isInStock">Is it in stock?</label>
        <input type="checkbox" name="isInStock" <% if (game.isInStock) { %>checked<% } %>/>
        <button type="submit">Update Game</button>
    </form>
    <a href="/fruits">Back to Games</a>
</body>
</html>